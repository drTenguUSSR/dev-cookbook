# cookbook-github

## tmp1

\#bbb. удалить (сократить) манипуляции с RequestContextHolder для IT

Рецензия на книгу «Изучаем Git: пошаговое руководство с наглядными примерами»
https://habr.com/ru/companies/ssp-soft/articles/844980/

Как конфигурировать Git
https://habr.com/ru/articles/886538/

### Finding Unused Gradle Dependencies

https://www.baeldung.com/gradle-finding-unused-dependencies
https://github.com/nebula-plugins/gradle-lint-plugin

## G25-1. Схема именования веток и коментарии к комитам (на базе GitHub Flow)

### G25-1. Ветки 

имя ветки := prefix + "/" + bbbb

prefix := "feature" | "release" | "zzz"

- feature - вся разработка или изменения:
  - если доработка успешна: merege в main, ветка удаляется
  - если доработка неуспешна, но содержит потенциально пелезный код - переименовывается из feature/bbbb в zzz/bbbb
  - если доработка неуспешна и полезного кода нет - ветка удаляется. в issue коментарий
- release - только релизные версии. не удаляются.
- zzz - замороженные изменения. формируются из feature/bbbb. не удаляются

bbbb := номер issue из GitHub

### G25-1. Коммит

имя коммита := "#" + bbbb + ". " + description

bbbb := номер issue из GitHub

description := описание конкретного изменения в этом комите

### Пример ветка + коммит

1. в github есть issue номер 1220
2. в git создается ветка "feature/1220"
3. все комиты связаные с этой веткой именуются как "#1220. Добавлен контроллер /api/public/hello"
4. по окончании работы feaure/1220 мержится в main ветку и ветка feaure/1220 удаляется.

Примечание: нужно писать в коментарии именно "#1220". Результат внутри 
issue будет добавляться информация.

## быстрый/простой коммит

```cmd
git checkout -b feature-branch
echo "new content" > file.txt
git add file.txt
git commit -m "Add new content"

git checkout main 
git branch -d feature-branch
```

## закрытие issue через коментарий к коммиту

нужно:
- отправить коммит с коментарием содержащис одно из слов close,
closes, closed, fix, fixes, fixed, resolve, resolves, resolved и ссылку на issue
напимер "закрытие fix #4"
- коммит должен быть слит (merged) в ветку по умолчанию (обычно main или master)

## Git-консоль. вывод настроек

- [книга git-scm.com](https://git-scm.com/book/ru/v2)
- вывод всех текущих настроек, включая системные, глобальные и локальные, 
причем локальные настройки имеют приоритет над глобальными и системными. 
```cmd
git config --list
```
- просмотр источника каждой настройки
```cmd
git config --list --show-origin
```
- просмотр конфигов системный/глобальный/локальный
```cmd
git config --system --list
git config --global --list
git config --local --list
```

## Как создать ветку средствами github от конкретного коммита?

На сайте GitHub напрямую через веб-интерфейс нельзя создать новую ветку
 от конкретного коммита, если вы не используете GitHub Desktop или 

- Перейдите по ссылке на нужный коммит, например
- На странице коммита найдите кнопку с надписью <> «Browse files» — она находится справа от хэша коммита.
- Нажмите на неё — вы перейдёте на просмотр файлов репозитория на момент этого коммита.
- На этой странице в левом верхнем углу вы увидите выпадающий список с названием текущей ветки (или коммита). Нажмите на него.
- В поле ввода введите новое имя ветки, например "my-new-branch-from-commit".
- GitHub автоматически предложит создать новую ветку с этим именем, указывающую на данный коммит — нажмите «Create branch: my-new-branch-from-commit».

⚠️ Важно: этот способ работает только если вы имеете права на запись в репозиторий (то есть вы владелец или у вас есть права push). 

Альтернативно, в GitHub Desktop можно просто кликнуть правой кнопкой по коммиту в истории и выбрать «Create branch from commit»

## Выбрать для нового issue существующую ветку?

Вот как это сделать:

- Откройте нужный issue.
- Прокрутите вниз до секции «Development» (она может быть свёрнута).
- Нажмите «Link a branch» или аналогичную кнопку.
- В появившемся поле начните вводить имя существующей ветки, и выберите её из списка.

!NB: можно привязать к одному issue несколько веток

## настроить Git-клиент доступ к GitHub.com с помощью Personal access tokens (Qwen-3 max)

*хинт*: способ подходит только для репозиториев доступных по протоколу http и https. 
если доступ по протоколу git - используются ssh ключи

- создать токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) или Fine-grained tokens.
  - Scopes: repo — полный доступ к приватным и публичным репозиториям
  - Скопируйте токен сразу — он больше не будет показан!
- Использование HTTPS с токеном
  - git clone https://github.com/ваш-логин/ваш-репозиторий.git
  - При запросе:
    - Username: ваш GitHub-логин (например, myusername)
    - Password: вставьте Personal Access Token
- Настройка Git Credential Helper (чтобы не вводить токен каждый раз)
  Windows: git config --global credential.helper wincred
  Linux (если установлен libsecret): git config --global credential.helper libsecret
  кеширование:
  ```bash
  git config --global credential.helper cache  # хранит в памяти 15 минут
  git config --global credential.helper 'cache --timeout=3600'  # 1 час
  ```
- Если вы уже клонировали репозиторий и он использует старый URL без токена:
  ```bash
  git remote set-url origin https://github.com/ваш-логин/ваш-репозиторий.git
  ```
- проверка
  ```cmd
  git pull
  # или
  git push
  ```
  результат:
  ```text
  результат "Hi <ваш-логин>! You've successfully authenticated..."
  ```

### примечания
- Если используете 2FA (двухфакторную аутентификацию) — это не мешает работе с PAT, наоборот, PAT — официальный способ аутентификации при включённой 2FA.
- 403 Forbidden: токен неверный, просрочен или не имеет нужных прав (repo scope).
- Password authentication is not available: вы пытаетесь использовать пароль вместо токена.
- Token в истории bash: избегайте вставки токена в командную строку напрямую.

## Hастроить Git-клиент доступ к GitHub.com с помощью ssh (Qwen-3 max) - через ppk

1. генерация/открытие ключа 
- запустить puttyGen
- создать или открыть ключ ppk - рекомендуется Ed25519 или RSA-4096 
  ← github-drTenguUSSR-pvt.ppk (ECDSA)
  fingerprint `SHA256:534950345936456456asdadafsfsdklfjefjslkdfj0`
- ? сохраните приватный ключ как id_ed25519.ppk или id_rsa.ppk → id_rsa.ppk
- Скопируйте содержимое поля "Public key for pasting into authorized_keys file" — оно понадобится позже. → github-drTenguUSSR-pvt.txt
  - Формат: OpenSSH: 
    - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... ваш@email.com
    - ssh-rsa AAAAB3NzaC1yc2EAAA... ваш@email.com
  - используется в файлах ~/.ssh/authorized_keys на серверах.
  - Принимается GitHub, GitLab, Bitbucket и другими Git-хостингами.
  - это одна строка, начинается с ssh-rsa, ssh-ed25519 и т.п.
  - ✅ именно этот текст нужно копировать и вставлять в GitHub → SSH keys.
2. Экспортируйте ключ в OpenSSH-формат
- В меню выберите: Conversions → Export OpenSSH key: -> C:\Users\<ваш-пользователь>\.ssh\id_rsa
  содержание "BEGIN RSA PRIVATE KEY"
- нажать "Save public key" → сохраните как id_ed25519.pub (или id_rsa.pub) -> id_rsa.pub
  - сохраняет файл с расширением .pub
  - формат: PuTTY  (не OpenSSH!): многострочный. начало "---- BEGIN SSH2 PUBLIC KEY ----", конец "---- END SSH2 PUBLIC KEY ----"
  - используется только PuTTY и связанными инструментами (Pageant, plink).
  - НЕ принимается GitHub
  - 🔴 Не используйте файл, созданный через «Save public key», для GitHub!
3. Настройка папки - C:\Users\<ваш-пользователь>\.ssh\
  - Поместите туда id_rsa
  - Установите правильные права (в Git Bash):
```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_rsa
chmod 644 ~/.ssh/id_rsa.pub
```
    прим: В Windows эти права не так критичны, но Git Bash может ругаться, если права слишком открытые. 
4. Добавьте публичный ключ в GitHub
- Скопируйте содержимое файла id_ed25519.pub (целиком, одна строка).
- Перейдите на GitHub → Settings → SSH and GPG keys → New SSH key.
- Вставьте ключ, укажите имя (например, My Windows PC), тип — Authentication.
- Нажмите Add SSH key.

5. Проверьте подключение `ssh -T git@github.com`
  результат "Hi <ваш-логин>! You've successfully authenticated..."

6. Используйте SSH-адреса репозиториев
- Клонируйте репозитории по SSH:
  `git clone git@github.com:ваш-логин/ваш-репозиторий.git`
- ИЛИ измените существующий remote:
  `git remote set-url origin git@github.com:ваш-логин/ваш-репозиторий.git`
  
*Дополнительно*: Использование Pageant (если хотите)
Если вы всё же хотите использовать Pageant (SSH-агент от PuTTY):
- Загрузите .ppk-файл в Pageant.
- Настройте Git for Windows использовать PuTTY/plink вместо OpenSSH:
```bash
git config --global core.sshCommand "plink.exe"
```
Но это сложнее, менее совместимо и не рекомендуется, особенно если вы работаете и из bash, и из cmd.
📌 Лучше придерживаться OpenSSH-ключа, как описано выше — это стандарт де-факто. 

### как git-клиент определяет нужный файл при соединении с github в папке .ssh?

Git-клиент сам по себе не определяет SSH-ключи — он делегирует всю работу 
SSH-клиенту (обычно ssh, входящему в состав OpenSSH). Именно SSH-клиент 
решает, какой ключ использовать при подключении к git@github.com.
Как SSH определяет, какой приватный ключ использовать?

SSH ищет ключи в следующем порядке:
1. если `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_custom" git clone ...`
  — тогда используется указанный файл.
  Но по умолчанию этого нет.
  
2. Настройки в ~/.ssh/config
  Если у вас есть файл ~/.ssh/config, SSH читает его и применяет правила.
```config
Host github.com
  IdentityFile ~/.ssh/id_ed25519_work
  IdentitiesOnly yes
```
  → При подключении к github.com будет использован только id_ed25519_work.
  ✅ Это лучший способ управлять несколькими ключами (личный/рабочий аккаунт и т.д.).

3. Стандартные имена файлов (по умолчанию)

	Если нет ~/.ssh/config, SSH пробует заранее определённые имена приватных ключей в таком порядке:
```bash
  ~/.ssh/id_ed25519
  ~/.ssh/id_ecdsa
  ~/.ssh/id_ecdsa_sk
  ~/.ssh/id_rsa
  ~/.ssh/id_dsa
```
  ⚠️ Обратите внимание: именно приватные ключи без расширения (не .pub!). 
  SSH будет по очереди пробовать эти ключи, пока один из них не подойдёт (или не исчерпает попытки).

4. SSH Agent (ssh-agent)
Если вы запустили ssh-agent и добавили ключи через ssh-add, то SSH будет 
использовать только ключи из агента, даже если они не в стандартных именах.
```bash
eval $(ssh-agent)
ssh-add ~/.ssh/my_custom_key
```
Теперь при подключении к GitHub будет использоваться my_custom_key, даже если его имя не id_rsa.

💡 Но если в ~/.ssh/config указано IdentitiesOnly yes, то только ключи из IdentityFile, даже если в агенте есть другие. 

### Как проверить, какой ключ используется?

Запустите с подробным логом:
```bash
ssh -vT git@github.com
# В выводе вы увидите строки вроде:
debug1: Offering public key: /home/user/.ssh/id_ed25519 ED25519 SHA256:...
debug1: Server accepts key: ...
# Или, если ключ не подошёл:
debug1: Offering public key: ...
debug1: Authentications that can continue: publickey
#Это покажет, какие ключи SSH пытается использовать и какой был принят.
```

Если у вас один ключ — назовите его id_ed25519 или id_rsa, и всё заработает автоматически.
Если ключей несколько — настройте ~/.ssh/config.

```ssh-config
Host github.com
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519
  IdentitiesOnly yes
```

### пример `~/.ssh/config` два эккаунта

- конфиг 
```ssh-config
# Личный аккаунт GitHub
Host github.com-personal
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_personal
  IdentitiesOnly yes

# Рабочий аккаунт GitHub
Host github.com-work
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_work
  IdentitiesOnly yes
```

🔹 github.com-personal и github.com-work — это псевдонимы (алиасы), которые вы сами придумываете.
🔹 HostName github.com — настоящий адрес сервера.
🔹 IdentitiesOnly yes — запрещает SSH использовать другие ключи из агента или по умолчанию. 

- Используйте правильные URL при клонировании

```bash
git clone git@github.com-personal:personal-user/my-project.git
git clone git@github.com-work:work-org/internal-tool.git
```

💡 Обратите внимание: после git@ идёт ваш алиас, а не github.com. 

-  Если репозиторий уже клонирован — измените remote

```bash
# Для личного проекта
git remote set-url origin git@github.com-personal:personal-user/my-project.git

# Для рабочего проекта
git remote set-url origin git@github.com-work:work-org/internal-tool.git
```

- ✅ Проверка подключения

```bash
# Проверить личный аккаунт
ssh -T git@github.com-personal
# Должно быть: Hi personal-user! You've successfully authenticated...

# Проверить рабочий аккаунт
ssh -T git@github.com-work
# Должно быть: Hi work-org-bot! You've successfully authenticated...
```

- 💡 Дополнительно: настройка Git user.name/user.email

```bash
# В личном репо
git config user.name "Personal Name"
git config user.email "personal@email.com"

# В рабочем репо
git config user.name "Work Name"
git config user.email "work@company.com"
```

??? Или используйте условные include в глобальном .gitconfig (начиная с Git 2.13).

## git-настройки `.git/config`
-crlf?
-rebase?

## вывод диаграммы ганта?

## имя ветки с кириллицей?

## GPG: создание, установка в GitHub, отзыв сертификата

## искать в Idea в Git Log по RegExp по коментарию?

работает.
непосредственно в git/Show Git Log

##  Intellij Idea. GitLog настроить как отдельную "вкладку"

- git/Show Git Log
- ПКМ на панели инструментов (⚙️)
  - move to: `right bottom`
  - view mode: undock
  - resize: Stretch to Top
  - resize: Stretch to Bottom
- результат: в правом нижнем углу Sidebar

##  Intellij Idea. Переход по id-комита

- alt-9
- ctrl-f и ввести id-комита

## Intelij Idea. Отображение сопоставления локальной и remote ветки

- alt-f12
```cmd
git branch -vv
git remote show origin
```

список только remote ветки
```cmd
git branch -r
```

## Intelij Idea. переименование ветки локально и remote

### Переименование локальной ветки
- Через интерфейс IntelliJ IDEA:
  - Откройте панель «Ветви» (Git branches) или используйте всплывающее окно «Ветви».
  - Кликните правой кнопкой мыши по ветке, которую хотите переименовать.
  - Выберите пункт «Переименовать» (Rename).
  - Введите новое имя ветки и нажмите Enter.
- Через терминал (внутри IDEA):
  - Если вы находитесь на ветке, которую нужно переименовать:
```cmd
git branch -m <новое-имя>
```
  - Если вы находитесь на другой ветке:
```cmd
git branch -m <старое-имя> <новое-имя>
```

### Переименование удаленной ветки
IntelliJ IDEA не предоставляет встроенной функции для автоматического
переименования удаленной ветки. Это нужно делать вручную,
выполнив следующие шаги:
- Удалите старую удаленную ветку.
  - Откройте терминал в IntelliJ IDEA (View -> Tool Windows -> Terminal| Alt-F12).
  - Выполните команду:
```cmd
git push origin --delete <старое_имя_ветки>
```
- Отправьте новую локальную ветку на удаленный сервер.
  - Чтобы отправить локальную ветку с новым именем, выполните команду:
```cmd
git push origin -u <новое_имя_ветки>
```
